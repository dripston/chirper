"""
Echo Chamber — LLM client wrapper (placeholder).

This module will provide a thin abstraction over the **Groq** inference
API, which serves as the primary LLM provider for all agent reasoning:
generating comments, reposts, distortions, and direct messages.

The wrapper will handle rate-limiting, retries, and a mock mode
(toggled via ECHO_CHAMBER_MOCK=1) for local development without
burning API credits.

Implementation planned for Phase 2.
"""
